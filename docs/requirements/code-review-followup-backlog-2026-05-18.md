# コードレビュー後続バックログ

- 対象リポジトリ: `tanaka03-sketch/roulette`
- 作成日: 2026-05-18
- 元資料: `docs/requirements/code-review-2026-05-18.md`
- 方針: 利用者影響と修正コストのバランスから、迷わず着手できる順に並べる

## 優先順位サマリー

| Priority | テーマ | 目的 | 対応 Issue | 状態 |
| --- | --- | --- | --- | --- |
| P1 | 同名候補の識別性改善 | 誤編集・誤削除の予防 | #28 | 完了 |
| P1 | 候補名バリデーションの共通化 | 入力制約の一貫性確保 | #29 | 完了 |
| P2 | 抽選済み状態の復元テスト追加 | FR-015 の回帰防止 | #30 | 完了 |
| P3 | モバイル自動検証の導入 | レイアウト回帰の早期検知 | #31, #32 | ブロック中 |

## 推奨タスク一覧

### 1. 同名候補を識別できる UI 仕様を追加する

- 優先度: P1
- 種別: UX / Requirements
- 対応 Issue: #28
- 状態: 完了
- 目的: 同名候補が並んでも利用者が個別に操作できるようにする
- 完了内容:
  - 要件書に識別仕様を追加した
  - 初期版の識別方式として登録順番号を採用した
  - 編集・削除ラベルにも識別情報を含める方針を反映した

### 2. 候補名バリデーションを共通化する

- 優先度: P1
- 種別: Quality
- 対応 Issue: #29
- 状態: 完了
- 目的: 追加・編集・将来拡張で同じ制約を保証する
- 完了内容:
  - `src/domain` に候補名検証関数を追加した
  - 追加と編集の両方で同じ検証関数を使う形にそろえた
  - 最大 120 文字制限をロジックで保証する形にした

### 3. 抽選済み状態の復元テストを追加する

- 優先度: P2
- 種別: Testing
- 対応 Issue: #30
- 状態: 完了
- 目的: 保存形式の変更で FR-015 を壊さないようにする
- 完了内容:
  - 抽選後の再読み込みで `抽選済み` 表示が復元されることをテストで固定化した
  - 抽選対象件数の復元確認も追加した

### 4. スマートフォン対応の自動検証方針を追加する

- 優先度: P3
- 種別: Testing
- 対応 Issue: #31
- 状態: 方針整理完了
- 目的: 狭幅 UI の回帰を自動で検知する
- 完了内容:
  - `docs/requirements/mobile-verification-plan-2026-05-18.md` を追加した
  - 手動確認基準から Playwright 導入へつなぐ段階的方針を整理した
  - CI 組み込みの考え方まで文書化した

### 5. Playwright を導入してモバイル E2E と任意 CI ジョブを追加する

- 優先度: P3
- 種別: Testing / CI
- 対応 Issue: #32
- 状態: ブロック中
- 目的: 方針整理済みのモバイル検証を実際の自動テストとして実装する
- 想定内容:
  - `@playwright/test` 導入
  - 最小モバイル E2E 追加
  - 任意 CI ジョブ追加
- ブロック理由:
  - ランタイムには `playwright` 本体があるが、Playwright 用ブラウザ実体が未配置
  - `chromium.launch()` は `/root/.cache/ms-playwright/...` の実行ファイル不足で失敗する
  - `npm` / `npx playwright install` による取得が `403 Forbidden` で停止する
- 必要条件:
  - npm registry と Playwright browser download が可能な環境

## 着手順のおすすめ

1. 外部依存取得が可能な環境で #32 を再開する
2. それまでは依存追加不要の改善を先に進める

## 最小残作業パッケージ案

残っている主要項目は、モバイル自動検証の実装段階のみである。

1. Playwright 設定追加
2. 最小モバイル E2E 追加
3. CI への任意ジョブ追加

## 備考

- 当初の P1 と P2 は、コード修正と要件補強まで対応済み
- #31 は方針整理まで完了している
- 実装作業は #32 で継続する
- 現在は実行環境の依存取得制限により停止中
